language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-js-browser-module' ]; then cd .. && eval "mv $currentfolder generator-js-browser-module" && cd generator-js-browser-module; fi
